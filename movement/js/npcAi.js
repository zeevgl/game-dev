// function checkColisionWithPlayer(player, npc) {
//   if (
//     npc.x < player.x + player.width &&
//     npc.boxX > player.x &&
//     npc.y < player.y + player.height &&
//     npc.boxY > player.y
//   ) {
//     return true;
//   }

//   return false;
// }

// function npcAI(player, npc) {
//   npc.speedV.vx = player.x > npc.x ? 1 : -1;
// }

// function isNPCInScreen(player, npc, gameWidth) {
//   return Math.abs(player.boxX - npc.x) < gameWidth;
// }
